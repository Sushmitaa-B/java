//sorting and searching

1.//Linear search
import java.util.*;

public class Main {
    public static void main(String[] args) {
      Scanner sc= new Scanner(System.in);
      int n= sc.nextInt();
      int arr[]=new int[n];
      for(int i=0;i<n;i++){
        arr[i]=sc.nextInt();
      }
      int search=sc.nextInt();
      Boolean indicate=false;//int c=0;
      //for(int i=n-1;i>=0;i--){//used to search from last of an array 
      for(int i=0;i<n;i++){
        if(arr[i]==search){
          indicate=true;//c=1;
          System.out.println("search value "+search +" present at "+i+" index");//index wise
          System.out.println("search value "+search +" present at "+(i+1)+" position");//position wise
          break;//used to return the index of an first occurring element in an array .for eg if an array has 3 4 5 3 2 
          // then the search element is also 3 then if break is used to return the i st find elkement index and then it stops. 
        }
      }
      if(indicate==false){//c==0
        System.out.println("search value "+search+" not present in an array");
      }
      
    }
}
   
5
1 2 3 4 5 
0
Output:
search value 0 not present in an array

  or 

  
5
1 2 3 4 5 
3
Output:
search value 3 present at 2 index
search value 3 present at 3 position

2.//binary search
//unknown iteration
import java.util.*;

public class Main {
    public static void main(String[] args) {
      Scanner sc= new Scanner(System.in);
      int n= sc.nextInt();
      int arr[]=new int[n];
      for(int i=0;i<n;i++){
        arr[i]=sc.nextInt();
      }
      Arrays.sort(arr);//used to sort the array (built in method)
      for(int x:arr){
        System.out.print(x+" ");
      }
      System.out.println();
      int search=sc.nextInt();
      int start=0,end=n-1;
      Boolean indicate=false;
      while(start<=end){
        int mid =(start+end)/2;
        if(arr[mid]==search){
          System.out.println("search element "+ search+" present in "+mid+" index");
          indicate=true;
          break;
        }
        else if(search>arr[mid]){//if the search element value is greater than mid value 
        // then the start point is mid+1,end point same 
          start=mid+1;
        }
        else{//if the search element value is lesser than mid value then
        // the end point becomes mid-1
          end=mid-1;
        }
        
      }
      if(indicate==false){
        System.out.println("search element "+ search+" not present in an array");
      }
      
    }
}



5
5 4 8 99 1 
0
Output:
1 4 5 8 99 
search element 0 not present in an array
(or)

5
5 4 8 99 1 
99
Output:
1 4 5 8 99 
search element 99 present in 4 index

3.//bubble sort Time Complexity=O (n^2)
import java.util.*;

public class Main {
    public static void main(String[] args) {
      Scanner sc= new Scanner(System.in);
      int n= sc.nextInt();
      int arr[]=new int[n];
      for(int i=0;i<n;i++){
        arr[i]=sc.nextInt();
      }
      for(int i=0;i<n;i++){
        for(int j=0;j<n-i-1;j++){//here we use j<n-i-1 ,to avoid the unneseccary execution of the line 
        //for eg we know that if we swap until the last index then that element must be the higher than all 
        //,so there is no need to rerun.First time it run to n-1 time and in second time n-2 and viceversa
          if(arr[j]>arr[j+1]){
            int t=arr[j];
            arr[j]=arr[j+1];
            arr[j+1]=t;
          }
         }
        
      }
      for(int x:arr){
       System.out.print(x+" "); 
      }
      
    }
}

5
5 4 8 99 1 
99
Output:
1 4 5 8 99 

4.//selection sort Time Complexity=O (n^2)
import java.util.*;

public class Main {
    public static void main(String[] args) {
      Scanner sc= new Scanner(System.in);
      int n= sc.nextInt();
      int arr[]=new int[n];
      for(int i=0;i<n;i++){
        arr[i]=sc.nextInt();
      }
      for(int i=0;i<n;i++){
        int minindex=i;//simply assuming that the value in 0 th index as min value
        for(int j=i+1;j<n;j++){//j=i+1 because we didn't compare the element with itself,
        // so we are comparing from the next index 
          if(arr[j]<arr[minindex]){//comparing arr[j] with arr[minindex] if arr[j]<arr[minindex] then 
          // assign minindex as j ie an index of the min value 
            minindex=j;
          }
        }
        int temp=arr[i];//swapping the min value with the assumed min value 
        arr[i]=arr[minindex];
        arr[minindex]=temp;
        
      }
      for(int x:arr){//print the elements
       System.out.print(x+" "); 
      }
      
    }
}


5
25 4 8 99 1 

Output:
1 4 8 25 99 

5.//insertion sort Time Complexity=O (n^2)
import java.util.*;

public class Main {
    public static void main(String[] args) {
      Scanner sc= new Scanner(System.in);
      int n= sc.nextInt();
      int arr[]=new int[n];
      for(int i=0;i<n;i++){
        arr[i]=sc.nextInt();
      }
      for(int i=1;i<n;i++){//i is initialized to 1 because in insertion sort ,
      // we need to take the key value at the 1 st index
        int key=arr[i];//used to store the key value safely and can be used later
        int j=i-1;//j is initialized to i-1 bcs for eg if i=1 then we need to check with the arr[0] 
        while(j>=0 && arr[j]>key){//here we are checking the condition that j is >0 and the element in arr[j]>key 
        // if so then assign the arr[j] to arr[j+1] for eg arr[0] is assigned to arr[1]
          arr[j+1]=arr[j];
          // j--; here we decrement the j ,so that we can assign the element in the arr[j] to the arr[j+1] ie the key element index
        }
        arr[j+1]=key;//and update the key element index with the key value
      }
      
      for(int x:arr){//print the elements
       System.out.print(x+" "); 
      }
      
    }
}  


//insertion sort Time Complexity=O (n^2)
import java.util.*;

public class Main {
    public static void main(String[] args) {
      Scanner sc= new Scanner(System.in);
      int n= sc.nextInt();
      int arr[]=new int[n];
      for(int i=0;i<n;i++){
        arr[i]=sc.nextInt();
      }
      for(int i=1;i<n;i++){
        int key=arr[i];//used to store the key value safely and can be used later
        int j=i-1;
        while(j>=0 && arr[j]>key){
          arr[j+1]=arr[j];
          j--;
        }
        arr[j+1]=key;
      }
      
      for(int x:arr){//print the elements
       System.out.print(x+" "); 
      }
      
    }
}
8
6 3 1 9 3 4 8 6
Output:

50 ms | 46.6 MB
1 3 3 4 6 6 8 9 
