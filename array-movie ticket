import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        Scanner x = new Scanner(System.in);

        System.out.println("Enter number of elements: ");
        int length = x.nextInt();  
        String[] arr = new String[length];

        System.out.println("Enter the elements: ");
        for (int i = 0; i < length; i++) {   
            arr[i] = x.next();  
            if (arr[i].equals("Priya")) {  
                arr[i] = "Paapu";  
            }
        }

        System.out.println("You entered:");
        for (String num : arr) {
            System.out.println(num);
        }
        int newLength = Math.max(0, length - 2); 
        String[] updatedArr = new String[newLength];

        for (int i = 0; i < newLength; i++) {
            updatedArr[i] = arr[i];
        }

        System.out.println("After deleting last two names:");
        for (String name : updatedArr) {
            System.out.println(name);
        }
    }
}








import java.util.Scanner;
public class Main
{
	public static void main(String[] args) {
	    Scanner option = new Scanner (System.in);
	    System.out.println("Enter your choice:");
        System.out.println("1. Enter the elements");
        System.out.println("2. Update teh elements");
        System.out.println("3. Remove the elements");
       
	    int choice = option.nextInt();
        

        switch (choice) {
            case 1:
                
                Scanner x= new Scanner(System.in);
                Scanner name= new Scanner(System.in);
                System.out.println("How many seats you want?:");
                int length = x.nextInt();  
                String[] arr = new String[length];

                System.out.println("Enter the seat no: ");
                for (int i = 0; i < length; i++) {   
                arr[i] = x.next();  
               
        }
                
                
                System.out.println("Enter the length:");
                int l = name.nextInt();  
                String[] ar = new String[l];

                
                for (int j = 0; j < l; j++) {   
                ar[j] = x.next();  
                
        }
                System.out.println("You entered:");
                for (String nam : ar) {
                System.out.println(nam);
        }
        for (int i=0;i<length;i++){
            for (int j=0;j<l;j++){
                System.out.println(arr[i]+ "" +ar[j]);
            }
        }
                
                
                
                break;
            case 2:
                Scanner r= new Scanner(System.in);
                System.out.println("enter the seat to be removed:");
                int remove = r.nextInt();
                Scanner h= new Scanner(System.in);
                System.out.println("enter height:");
                int height = h.nextInt();
                 break;
            case 3:
                // Scanner s= new Scanner(System.in);
                // System.out.println("enter side");
                // int side = s.nextInt();
                
                break;
            
            

        }
	}
}











